# Only needed to run the seed/enrich scripts locally.
# Not required at runtime — genre data is baked into the committed SQLite DB.

# Get a free key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# Alternative: https://www.omdbapi.com/apikey.aspx (just needs an email)
OMDB_API_KEY=
